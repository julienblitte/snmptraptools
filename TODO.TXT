/*

  TODO: snmptrapdispatcher
  - should load all plugins at start
  - when action, it should find the maching UID plugin and call LoadConfig and Run function
    * get data from plugin_get_configuration
  - in dispatcher, update configuration structure (wkdir and run -> replace by plugin UID)

  TODO: snmptrapconfig
  - should load all plugins at start
  - for each action, should propose the plugin in a listbox
  - save the UID in registry instead of wkdir and run values
  - add a button plugin parameter and call EditConfig function:
    * get data from plugin_get_configuration
    * call function EditConfig
    * set data to plugin_set_configuration

  TODO: plugins
  - implement EditConfig function
  - plugins to develop:
	* run a program
    * email alerting
    * HTTP request
    * log to csv (include a log rotate)
    * send to SQL
	* send to Windows Event

*/
